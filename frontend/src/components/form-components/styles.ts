import { styled } from "../../../globalStyles";

export const StyledForm = styled('div', {
    display: 'grid',
    marginInline: 'auto',
    position: 'relative',
    border: '1px solid gray',
    borderRadius: '5px',
    padding: '0.5rem',
} )