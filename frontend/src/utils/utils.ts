// export const sortArray = (arr:[]) => {
//     const notesPrevState = [...notes];
//       const note = notesPrevState.find((n, i) => i === indexOf);
//       notesPrevState.splice(indexOf, 1);
//       const rest = notesPrevState.splice(index);
//       rest.unshift(note!);
// }
